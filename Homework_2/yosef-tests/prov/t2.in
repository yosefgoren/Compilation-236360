bool uselessFunc(int x, int y)
{
    return x < y;
}

int return6()
{
    return 6;
}

void main() {
    const bool c = true;
    if (not c) {
        int x = return6();
        printi(x);
		x = (const int) e;
    }
    else
        syntax error;
}
